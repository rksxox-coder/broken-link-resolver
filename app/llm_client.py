# Placeholder for future free LLM integration
# (e.g. local model, Google search ranker, etc.)

def re_rank_urls(original, candidates):
    # For now, simply return first candidate
    return candidates[0] if candidates else None
